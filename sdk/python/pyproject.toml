[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "filevault"
version = "0.3.0"
description = "Python SDK for the FileVault Agent Storage API"
readme = "README.md"
license = "MIT"
requires-python = ">=3.9"
dependencies = []

[project.urls]
Homepage = "https://filevault.host"
Repository = "https://github.com/lohith261/filevault"
